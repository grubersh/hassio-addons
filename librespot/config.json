{
  "name": "librespot",
  "version": "v0.4.2",
  "slug": "librespot",
  "description": "spotify",
  "startup": "services",
  "boot": "auto",
  "map": ["share:rw"],
  "host_network": true,
  "host_dbus": true,
  "options": { 
    "librespotopts": "-v -n Alle -b 320 --device-type=speaker --backend pipe --device /share/snapfifo/librespot"
  },
  "schema": {
    "librespotopts": "str"
   },
  "arch": ["armhf", "aarch64", "amd64"]
  }
